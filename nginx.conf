server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    # This is the single line that fixes the problem.
    # It tells Nginx:
    # 1. Try to find a file matching the exact URL ($uri).
    # 2. If not found, try to find a directory matching the URL ($uri/).
    # 3. If both fail, serve the /index.html file as a fallback.
    try_files $uri $uri/ /index.html;
  }

  # Caching for static assets
  location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
  }

  # Security headers
  add_header X-Frame-Options "SAMEORIGIN" always;
  add_header X-Content-Type-Options "nosniff" always;
  add_header X-XSS-Protection "1; mode=block" always;

  # Gzip compression
  gzip on;
  gzip_vary on;
  gzip_min_length 1024;
  gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
}
